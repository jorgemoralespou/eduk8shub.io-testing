<div class="row">
    <div class="col-md-8">
        <article class="post" itemscope itemtype="http://schema.org/BlogPosting">

            <header class="post-header">
                <h1 class="post-title" itemprop="name headline">{{ .Title }}</h1>
                <p class="post-meta"><time datetime='{{ .Date.Format "2006-01-02T15:04:05Z07:00" }}'
                        itemprop="datePublished">{{ .Date.Format "January 2, 2006" }}</time>{{ with .Params.Author }} •
                    <span itemprop="author" itemscope itemtype="http://schema.org/Person"><span
                            itemprop="name">{{ . }}</span></span>{{ end }}</p>
            </header>

            <div class="post-content" itemprop="articleBody">
                {{ if .Content }}
                {{ .Content }}
                {{ else }}
                <p>If you want to review the workshop content, you can browse the files and subdirectories under <a
                        href="{{ .Params.githuburl }}/tree/master/workshop/content">workshop/content</a>.</p>

                <h2 id="install-the-workshop">Install the workshop</h2>
                <p>To deploy the workshop, <a href="/pages/install/">install the eduk8s operator</a>, then run:</p>
                <div class="highlight">
                    <pre
                        style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4"><code class="language-bash" data-lang="bash">{{ .Params.install }}</code></pre>
                </div>

                {{ if .Params.install_notes}}
                <aside class="box">
                    <div>
                        <p>{{ .Params.install_notes }}</p>
                    </div>
                </aside>
                {{ end }}
                <h2 id="delete-the-workshop">Delete the workshop</h2>
                <p>To delete the workshop when finished, run:</p>
                <div class="highlight">
                    <pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:2;-o-tab-size:2;tab-size:2"><code class="language-bash" data-lang="bash">{{ .Params.delete }}
            </code></pre>
                </div>
                <h2 id="example-workshop">Example workshop</h2>
                <p>If you want to manually create an instance of the workshop, just create an instance of this CRD with
                    the following content:</p>
                <div class="highlight">
                    <pre style="color:#f8f8f2;background-color:#272822;-moz-tab-size:2;-o-tab-size:2;tab-size:2"><code class="language-yaml" data-lang="yaml">{{ .Params.example }}
            </code></pre>
                </div>
                {{ end }}
            </div>
        </article>
    </div>

    {{ partial "install.html" . }}
</div>


{{ end }}